// Client-side Supabase config.
// Replace the placeholder values with your project's URL and anon (public) API key.
// These globals are read by the auth modules on the sign-in/up pages.
window.SUPABASE_URL = window.SUPABASE_URL || 'https://YOUR-PROJECT.supabase.co';
window.SUPABASE_ANON_KEY = window.SUPABASE_ANON_KEY || 'YOUR-ANON-KEY';
